# Codex Lumo Desktop

Windows 透明置顶悬浮窗。应用只读监听本机 `%USERPROFILE%\.codex\sessions` 与 `logs_2.sqlite`，将 Codex 任务生命周期映射为宠物状态。

## 使用

- 双击安装包，或直接运行便携版 `Codex-Lumo-1.0.0-x64.exe`。
- 点击悬浮窗展开详情；拖动窗口可改变位置。
- 托盘菜单可以隐藏窗口、开启鼠标穿透或设置开机启动。
- 鼠标穿透后按 `Ctrl + Alt + L` 召回窗口。

所有日志解析都在本机完成。应用不会显示内部推理文本，也不会上传 Codex 日志。
